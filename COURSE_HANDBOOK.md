# 📘 AlgoBank Course Handbook — Master File

> **Этот файл — единая точка входа для продолжения обучения.**
> Содержит всю историю курсов, статистику, профиль ученика и **полную программу AlgoBank**.

---

## 🧭 Как пользоваться этим файлом

Если ты — новый AI-ассистент, и ученик скинул тебе **только этот файл** (история чата пустая):

1. **Прочитай ВЕСЬ файл** сверху вниз — это ~10 минут.
2. **Создай 6 файлов-дневника** в проекте САМ (шаблоны в самом низу файла, раздел «📝 Шаблоны файлов»): `HANDOFF.md`, `PROGRESS.md`, `STATS.md`, `LEARNING_LOG.md`, `OVERALL_STATS.md` (ссылка на SpringBootProject), `COURSE_HANDBOOK.md` (копия).
3. **Представься ученику**, кратко резюмируй, что уже сделано, и спроси, продолжать ли по плану.

---

## 👤 Ученик

| | |
|---|---|
| **Имя** | Василий |
| **Цель** | Устройство на работу Java-разработчиком (Middle, универсал — чтобы прошёл везде) |
| **Опыт** | JavaRush (4 года назад, год обучения, основы помнит частично); Spring Core ✅ (12 шагов, 17.2 ч, ~85%); Spring Boot в процессе (шаг 9/15, 21.7 ч, ~82%) |
| **Темп** | 3-4 часа в день |
| **Платформа** | Windows 11, IntelliJ IDEA, Maven 3.9.9, **JDK 23.0.1** (target в `pom.xml` — Java 21) |

---

## 🎯 Профиль обучения

### Что заходит
- **Длинные разборы с аналогиями** (грузчик/склад, повар/продукты, плагин/коробка)
- **Мини-экзамены по 1 вопросу за раз, БЕЗ вариантов ответа** (ученик сам формулирует)
- **Схемы ASCII-арт** для объяснения потоков и жизненных циклов
- **Объяснение «на пальцах» перед кодом** — сначала «зачем», потом «как»
- **Связь с реальным кодом** — ученик любит видеть в логах, как теория работает
- **Видеть, как ошибка → новая тема** — не чинить ошибку за него, а превращать её в обучающий момент
- **Проект-обёртка** — теория и алгоритмы привязаны к одному живому проекту (AlgoBank), а не абстрактные примеры
- **Алгоритмические задачи как часть каждого шага** — 2-3 задачи на шаг, привязаны к домену (банковские операции)

### Что НЕ заходит
- ❌ Готовый код классов целиком — давай только подсказки и направление
- ❌ Варианты ответа A/B/C/D — это тест, а не понимание
- ❌ **Утечка ответов в опросах и мини-экзаменах** — критично! «Идея на пальцах» (объяснение новой темы) может содержать ответы, это нормально. Но в **самих вопросах и пояснениях к ним** ответа быть не должно
- ❌ Угадывание времени — всегда `Get-Date`
- ❌ **Мало задач** — ученик явно просил больше (минимум 2-3 задачи на шаг, итого 60-80 за курс)

### Сильные стороны ученика
- ✅ Понимает аналогии сразу
- ✅ Не зубрит — пытается понять суть
- ✅ Применяет знания к новым задачам (синтез)
- ✅ **Ловит неточности в вопросах ассистента** — видно критическое мышление
- ✅ Комментирует педагогические ошибки ассистента

### Слабые стороны (над чем работать)
- ⚠️ **Путает похожине концепции без явных маркеров отличий** → тренировать **явное сравнение**
- ⚠️ **Иногда доверяет недавней памяти больше, чем источнику** → тренировка: при ответе проверять **пакет класса**, а не «похоже на название»
- ⚠️ **Ошибки в формулировках закрепляются** → тренировка: после неверного ответа — **повторить правильный ответ вслух/письменно** в конце сессии
- ⚠️ **Java Core помнит частично** (JavaRush 4 года назад) → в начале AlgoBank делаем **экспресс-повторение Java Core**, чтобы не спотыкаться на собесе

---

## 🎯 Концепция проекта AlgoBank

> **Главная идея:** всё, что нужно для собеса (Spring + Java Core + алгоритмы + SQL), изучается **внутри одного живого проекта** — банковского backend.

**Почему банковский домен:**
- ✅ Естественные структуры данных (счета, транзакции, иерархия клиентов)
- ✅ Алгоритмы из реальной жизни (обнаружение мошенничества, циклы переводов, топ клиентов)
- ✅ Понятные бизнес-фичи (регистрация, переводы, история, аналитика)
- ✅ Хорошо ложится на Spring (REST API, JPA, Security, Cache)
- ✅ Можно показать на собесе как pet-project

**Что покрывает:**

| Тема | Покрытие |
|------|----------|
| Java Core (повторение) | Шаги 1-4 |
| Spring Boot | Шаги 5-14 |
| SQL | Шаги 5-14 (встроено в JPA-шаги) + шаги 18-19 (отдельный блок) |
| Многопоточность + JVM | Шаги 15-17 |
| Структуры данных | Шаги 20-21 |
| Алгоритмы | На каждом шаге + шаги 22-24 |
| LeetCode-практика | Шаг 25 (финальный марафон) |
| Финальная шпаргалка | Шаг 26 |


## 📋 Программа курса AlgoBank (~26 шагов)

### 📚 Блок 1: Java Core экспресс-повторение (шаги 1-4)
> Цель: восстановить забытое с JavaRush, подтянуть до крепкого уровня для собеса.

1. **Шаг 1: Collections framework** — List/Set/Map внутри, ArrayList vs LinkedList, HashMap внутри (bucket, equals/hashCode), LinkedHashMap, TreeMap. Задачи: Two Sum (HashMap), группировка транзакций по типу.
2. **Шаг 2: Generics, equals/hashCode, immutability** — вариантность, PECS, контракт equals/hashCode, immutable-объекты (как сделать свой `Money` value class). Задачи: Group Anagrams, валидация IBAN (простая).
3. **Шаг 3: Stream API + Optional + лямбды** — `map/filter/reduce/collect`, группировка транзакций, Optional, method references. Задачи: Top-3 клиента по сумме переводов (Stream + heap).
4. **Шаг 4: Records, sealed, pattern matching (Java 21)** — современный Java, убираем бойлерплейт в DTO. Задача: парсинг банковской выписки через pattern matching.

### 🏦 Блок 2: AlgoBank — Spring Boot (шаги 5-14)
> Цель: построить полноценный backend банковского приложения, попутно разбирая теорию.

5. **Шаг 5: Старт проекта + REST basics** — `@SpringBootApplication`, `@RestController`, `@GetMapping`, embedded Tomcat. Задачи: FizzBuzz, "Hello Bank" (2 easy).
6. **Шаг 6: DTO + валидация** — `@Valid`, `@NotBlank`, `@RestControllerAdvice`, обработка ошибок. Задачи: палиндром IBAN, валидация номера карты (Luhn-алгоритм) (easy + medium).
7. **Шаг 7: JPA entities + репозитории** — `@Entity`, `@Id`, `@OneToMany`/`@ManyToOne` (Account ↔ Transaction). Задачи: поиск дубликатов транзакций (HashSet), дедупликация по сумме+дате (medium).
8. **Шаг 8: Транзакции (`@Transactional`)** — границы транзакций, propagation, rollback. Задачи: **обнаружение циклов в графе переводов** (DFS, medium) — реальная анти-фрод задача.
9. **Шаг 9: Spring Security basics** — `SecurityFilterChain`, BCrypt, in-memory users. Задачи: **проверка силы пароля** (жадный алгоритм, easy + medium).
10. **Шаг 10: JWT + реальная аутентификация** — генерация токена, фильтр, защита endpoints. Задачи: **base64-кодирование** токена вручную (easy), парсинг JWT без библиотеки (medium).
11. **Шаг 11: Spring Data queries** — JPQL, native, Specification, динамические фильтры для истории транзакций. Задачи: **Top-K самых крупных транзакций** (heap, medium), sliding window по датам (medium).
12. **Шаг 12: Кэширование** — `@Cacheable`, `@CacheEvict`, **написать свой LRU-кеш** (LinkedHashMap) и подключить как Spring Cache. Задача: **LRU Cache своими руками** (medium-hard) + классический LeetCode 146.
13. **Шаг 13: Actuator + логирование** — `/actuator/health`, `/actuator/metrics`, SLF4J, MDC для трейсинга запросов. Задачи: **парсинг access-лога** (split + HashMap, easy), топ-10 эндпоинтов по ошибкам (heap, medium).
14. **Шаг 14: Тестирование** — `@SpringBootTest`, `MockMvc`, Testcontainers (PostgreSQL), unit-тесты с JUnit 5. Задачи: **генератор тестовых транзакций** (random + Faker, easy), **параллельная генерация** (CompletableFuture, medium).

### ⚡ Блок 3: Многопоточность + JVM (шаги 15-17)
> Цель: понять, как Java работает под капотом — критично для Middle собеса.

15. **Шаг 15: Thread basics** — Thread, Runnable, synchronized, volatile, happens-before. Задачи: **producer-consumer** с `BlockingQueue` (medium), **race condition** демо + фикс.
16. **Шаг 16: Executor + Future + CompletableFuture** — thread pools, асинхронность, параллельные вызовы для агрегации данных из нескольких сервисов. Задачи: **merge-k-sorted-lists** через ExecutorService (medium-hard), параллельный расчёт балансов.
17. **Шаг 17: JVM internals** — heap/stack, GC (G1, ZGC), classloading, JIT. Задачи: **анализ heap dump** (чтение стектрейса), **memory leak** демо (статическая коллекция, easy-medium).

### 🗄 Блок 4: SQL для собеседования (шаги 18-19)
> Цель: уверенный SQL — это 70% вакансий.

18. **Шаг 18: Базовый SQL** — JOIN (INNER/LEFT/RIGHT/FULL), подзапросы, CTE, GROUP BY + HAVING. Задачи на реальных данных AlgoBank: "топ-клиенты по регионам", "счета без транзакций за 90 дней".
19. **Шаг 19: Оконные функции + индексы + оптимизация** — `ROW_NUMBER/RANK/DENSE_RANK`, `LAG/LEAD`, индексы (B-tree, composite), EXPLAIN. Задачи: "выписка по счёту с нарастающим итогом", "предыдущая транзакция клиента".

### 🧮 Блок 5: Структуры данных (шаги 20-21)
> Цель: фундамент для алгоритмов.

20. **Шаг 20: Массивы, списки, стеки, очереди, деки** — реализация на массивах, анализ операций O(1)/O(n). Задачи: **Valid Parentheses** (стек, easy), **Min Stack** (стек + доп. стек, medium).
21. **Шаг 21: Деревья, кучи, графы** — BST, heap, представления графов (adjacency list/matrix). Задачи: **BFS кратчайший путь в графе** (medium), **Kth Largest Element** (heap, medium).

### 🎯 Блок 6: Алгоритмы (шаги 22-24)
> Цель: набить руку на паттернах собеседовских задач.

22. **Шаг 22: Два указателя + скользящее окно** — классика LeetCode. Задачи: **Two Sum II** (easy), **Container With Most Water** (medium), **Longest Substring Without Repeating** (medium).
23. **Шаг 23: Рекурсия + backtracking** — генерация комбинаций, откат. Задачи: **Generate Parentheses** (medium), **Permutations** (medium), **Subsets** (medium).
24. **Шаг 24: Динамическое программирование + жадные алгоритмы** — основа для hard-задач. Задачи: **Climbing Stairs** (easy), **Coin Change** (medium), **Jump Game** (medium), **Activity Selection** (жадный, medium).

### 🏆 Блок 7: Финал (шаги 25-26)
> Цель: финальная подготовка, марафон, шпаргалка.

25. **Шаг 25: LeetCode марафон** — 10-15 задач mixed difficulty (easy/medium) подряд за одну сессию с таймером, как на реальном собесе. Фокус на типовые паттерны.
26. **Шаг 26: Финальная шпаргалка + mock-собеседование** — собираем все шпаргалки из `LEARNING_LOG.md` в один файл `INTERVIEW_CHEATSHEET.md`, прогоняем 10 вопросов в режиме "экзамен".

---


---

## 🎯 Профиль алгоритмических задач

### Распределение по сложности
- **Easy** — 40% (разминка, проверка базы)
- **Medium** — 50% (основной уровень собеса)
- **Hard** — 10% (топ-компании, опционально)

### Типовые паттерны, которые закроем
- HashMap / HashSet для O(1) lookup
- Two pointers (разнонаправленные, быстрый/медленный)
- Sliding window (фиксированный, динамический)
- Stack (монотонный, для скобок)
- BFS / DFS
- Binary search
- Heap / Priority Queue (Top-K)
- Backtracking (генерация комбинаций)
- Greedy (жадные)
- Dynamic programming (1D, 2D)
- LinkedList (reverse, cycle detection)
- Trees (BFS/DFS, BST)

---

## 🚨 Главное правило про git (обязательно)

**После каждого завершённого шага** (теория + задачи + мини-экзамен) — **обязательно**:

```bash
git add .
git commit -m "step N: <название шага>"
git push
```

Ученик явно попросил это в Spring-проекте. Это страховка от потери прогресса.

### Что коммитить
- Все изменения в `src/` (новый код, фиксы, задачи)
- Обновления в 6 файлах-дневниках
- Новые тесты
- Изменения в `pom.xml`, `application.yml`
- Миграции Flyway

### Что НЕ коммитить
- `target/` (артефакты сборки)
- `.idea/` (настройки IDE)
- Локальные секреты, `.env`
- `data/` (H2 файлы)

---

## 🧭 Как устроен каждый шаг (формат v2)

1. **Микро-шаг A** — ассистент даёт 1-2 абзаца теории + микро-задание ученику (часть проекта AlgoBank)
2. **Микро-шаг B** — следующий кусок теории + следующее задание
3. **Микро-шаг C** — теория + **алгоритмическая задача №1** (привязана к домену)
4. **Микро-шаг D** — **алгоритмическая задача №2** (часто medium)
5. **Мини-экзамен** — 1-3 вопроса на итоговое понимание (теория + задачи)
6. **Коммит** — ученик прислал все логи/скриншоты/решения, шаг закрыт

> ⚠️ **Алгоритмические задачи решаются учеником самостоятельно** в пакете `ru.algobank.algo.<step>`. Ассистент даёт **только условие + подсказки + направление**, но не пишет код задач целиком. Исключение: мелкие правки и шпаргалки в `*.md`.

---


## 📊 Общая статистика по всем проектам

> Источник правды — `OVERALL_STATS.md`. Здесь — копия для самодостаточности.

### Суммарное время

| Проект | Тема | Часов | Шагов | Балл |
|---|---|---|---|---|
| SpringCoreProject | Spring Core без Boot | 17.2 | 12 / 12 | ~85% |
| SpringBootProject | Spring Boot + смежные | 21.7+ | 9 / 15 (в процессе) | ~82% |
| **AlgoBank** | **Java Core повт. + Spring Boot + Алгоритмы + SQL** | **0** (старт сегодня) | **0 / 26** | — |
| **Итого** | | **38.9+** | **21+ / 53** | |

---

## 🔖 Контрольные точки (чеклист «готов к собесу на Middle»)

По окончании AlgoBank ученик должен уметь:

- [ ] Уверенно объяснить и применить Java Core (коллекции, Stream, Generics, Records) на собесе
- [ ] Написать REST API на Spring Boot с валидацией, обработкой ошибок, JWT
- [ ] Спроектировать JPA-модель (счёт ↔ транзакции), написать `@Transactional` сервис
- [ ] Защитить endpoints через Spring Security + JWT, применить `@PreAuthorize`
- [ ] Написать интеграционный тест с `@SpringBootTest` + Testcontainers
- [ ] Решить 60+ алгоритмических задач (easy/medium) и объяснить подход
- [ ] Написать SQL-запросы с JOIN, CTE, оконными функциями, объяснить план через EXPLAIN
- [ ] Объяснить многопоточность (synchronized, volatile, ExecutorService, CompletableFuture)
- [ ] Рассказать про JVM (heap/stack, GC, classloading)
- [ ] Пройти mock-собеседование без запинки (10 вопросов подряд)

---

## 🚀 Команды ученика

| Команда | Что делать |
|---|---|
| **«начинаю обучение»** | Зафиксировать время старта (`Get-Date`), продолжить с текущего шага |
| **«пауза»** | Проверить время (`Get-Date`), **не коммитить**, просто ждать |
| **«закончили на сегодня»** | Проверить время, обновить `STATS.md` и `HANDOFF.md`, **закоммитить и запушить** |
| **«коммитим как есть»** | Дозаписать прогресс в `LEARNING_LOG.md`, синхронизировать дневники, коммит + пуш |

---

## 📝 Шаблоны файлов (для нового ассистента)

> Если файлы потеряны — создать по этим шаблонам:

### `HANDOFF.md` — краткое резюме
- Суть курса, ученик, проект
- Текущий статус (шаг, время, баллы)
- Что сделано в последнем шаге
- Ссылка на другие файлы

### `PROGRESS.md` — навигация
- Где остановились, что дальше
- План курса (ссылка на COURSE_HANDBOOK)
- Правила работы с паузами и сессиями

### `STATS.md` — дашборд
- Общий прогресс (шаг N / 26)
- Затраченное время (по сессиям)
- Мини-экзамены (история)

### `LEARNING_LOG.md` — конспект теории
- Разборы шагов с теорией
- Шпаргалки для собеса (в конце файла)

### `OVERALL_STATS.md` — общая статистика
- Сводка по всем проектам ученика
- Сквозные пробелы
- Готовность к собесу (общая оценка)

### `COURSE_HANDBOOK.md` — этот файл

---

**Удачи в AlgoBank! 🚀**

*Файл создан 11.09.2026 при старте AlgoBank. Последнее обновление: 11.09.2026.*

